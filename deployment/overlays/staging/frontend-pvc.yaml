apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: golem-frontend-staging-pvc
  labels:
    app: golem
    environment: staging
    tier: frontend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
