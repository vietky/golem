apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golem-nginx-staging-ingress
  labels:
    app: golem
    environment: staging
    tier: frontend
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - game.anhtran.dev
    secretName: golem-tls-staging
  rules:
  - host: game.anhtran.dev
    http:
      paths:
      - path: /golem-test
        pathType: Prefix
        backend:
          service:
            name: golem-nginx-staging
            port:
              number: 80
