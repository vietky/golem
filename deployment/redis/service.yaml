apiVersion: v1
kind: Service
metadata:
  name: golem-redis
  labels:
    app: golem
    tier: cache
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: golem
    tier: cache
