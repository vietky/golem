apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - redis/pvc.yaml
  - redis/deployment.yaml
  - redis/service.yaml
  - golem-app/configmap.yaml
  - golem-app/deployment.yaml
  - golem-app/service.yaml
  - golem-app/nginx-deployment.yaml
  - firebase-auth.yaml
  # Gateway and HTTPRoute resources are managed separately on k3s server
