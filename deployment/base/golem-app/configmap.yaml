apiVersion: v1
kind: ConfigMap
metadata:
  name: golem-app-config
  labels:
    app: golem
    tier: backend
data:
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "8080"
  REDIS_ADDR: "redis.default.svc.cluster.local:6379"
  REDIS_DB: "0"
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"
