# GitHub Actions Secrets Configuration
# This file contains the configuration for setting up GitHub Actions secrets
# to trigger Jenkins deployments
#
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real credentials to git!

# Jenkins server URL (include http:// or https://)
# Example: http://157.66.101.66:8080
JENKINS_URL=http://your-jenkins-server:8080

# Jenkins username
# This is the username you use to log into Jenkins
JENKINS_USER=your_username

# Jenkins API token
# Generate this from: Jenkins -> User -> Configure -> API Token
JENKINS_TOKEN=your_api_token_here

# Jenkins job name (optional, defaults to golem-century-deploy)
# This should match the job name created by the Ansible playbook
JENKINS_JOB_NAME=golem-century-deploy
